# Contributing

## Workflow
1. Create an issue describing the change.
2. Open a branch: `feat/<name>` or `fix/<name>`.
3. Keep PRs small and testable.
4. Include before/after examples for content output changes.

## Commit style
- `feat:` new feature
- `fix:` bug fix
- `docs:` documentation only
- `chore:` maintenance
